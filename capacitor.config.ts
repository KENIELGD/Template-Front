import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Opencity.Frontend.MeSubo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
